#version 330 core
precision highp float;
precision highp int;

in vec3 vertex_pos;
uniform mat4 pvMatrix;

void main() {
//    gl_Position = vec4(vertex_pos, 1.0);
    gl_Position = pvMatrix * vec4(vertex_pos, 1.0);
}
